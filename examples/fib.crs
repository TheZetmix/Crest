[!include] {
    "stdio.h"
}

var iterations : int = 20;

fn fib(n : int) : int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() : int {
    for (i : int = 0; i < iterations; ++i) {
        printf("%d\n", fib(i));
    }
}
