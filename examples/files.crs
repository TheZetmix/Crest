[!include] {
    "stdio.h"
    "stdlib.h"
}

fn main() : int {
    var fd : *FILE = fopen("./examples/calc.crs", "r");
    if !fd {
        printf("cant open file\n");
        exit(0);
    }
    var c : int;
    while (c = fgetc(fd)) != EOF {
        printf("%c", c);
    }
}
